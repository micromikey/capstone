{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"chmod -R 777 /app/storage /app/bootstrap/cache && php artisan key:generate --force --no-interaction || true && php artisan migrate --force && php artisan storage:link && php artisan config:clear && /usr/bin/supervisord -c /etc/supervisor/conf.d/supervisord.conf\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/up",
    "healthcheckTimeout": 100
  }
}
